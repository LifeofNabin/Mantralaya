server:
  port: 8080

spring:
  application:
    name: request

  datasource:
    url: jdbc:h2:mem:phantoncontactdb
    driver-class-name: org.h2.Driver
    username: sa
    password: admin123

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:khakendra1999@gmail.com}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  contact:
    from-email: khakendra1999@gmail.com
    internal-email: kbaduwal95@gmail.com

logging:
  level:
    org.pantheon_solutions: DEBUG
    org.springframework.mail: DEBUG