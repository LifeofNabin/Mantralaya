server:
  port: 8080

spring:
  application:
    name: request

  datasource:
    url: jdbc:h2:mem:phantoncontactdb
    driver-class-name: org.h2.Driver
    username: sa
    password: admin123

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  contact:
    from-email: noreply@tcs.com
    internal-email: contact@tcs.com

logging:
  level:
    org.pantheon_solutions: DEBUG
    org.springframework.mail: DEBUG